<%= form_for(@categorias_tipo) do |f| %>
  <% if @categorias_tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categorias_tipo.errors.count, "error") %> prohibited this categorias_tipo from being saved:</h2>

      <ul>
      <% @categorias_tipo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>
  <div class="field">
 <%= select_tag "tipo_accs", options_from_collection_for_select(TipoAcc.all, :id, :nombre,@sel), :multiple => true %>
	  </div>
<div class="field">
 <%= select_tag "estados", options_from_collection_for_select(Estado.all, :id, :nombre,@selEst), :multiple => true %>
	  </div>
  <div class="actions">
    <%= f.submit "ENVIAR.." %>
  </div>

<% end %>
